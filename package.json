{
  "name": "dango-battle-server",
  "version": "0.2.0",
  "private": true,
  "description": "DANGO BATTLE - Express + Socket.IO server (STEP 2: server bootstrap)",
  "main": "server/server.js",
  "scripts": {
    "start": "node server/server.js",
    "app:prepare": "node scripts/prepare-app.js",
    "app:android:init": "npm run app:prepare && npx cap add android",
    "app:sync": "npm run app:prepare && npx cap sync android",
    "app:open": "npx cap open android"
  },
  "dependencies": {
    "express": "^5.2.1",
    "socket.io": "^4.8.3",
    "@capacitor/core": "^8.0.0",
    "@capacitor/android": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0"
  }
}
