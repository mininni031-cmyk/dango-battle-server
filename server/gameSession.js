// DANGO BATTLE - 対戦セッション（STEP 8〜9以降で実装）
//
// ここに実装していく予定：
// - READY / 3・2・1カウントダウン（サーバー基準時刻）
// - 団子配置イベント（tier, x, width, stackIndex）の受信とtowerHeight/skillGauge更新
// - サーバー側の権威判定（段数の矛盾チェック、必殺技発動可否、勝敗確定）
// - 再戦フロー（rematchRequested管理、同一ルーム再開）
//
// STEP 2の現時点ではサーバー起動確認のみのため、中身は空です。

module.exports = {
  // 次のSTEPで GameSession クラス／createSession()／applyDropResult() 等をここに追加する。
};
